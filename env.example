# Database Configuration
# Option 1: Use connection string
# DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
# Example for Supabase:
# DATABASE_URL=postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require

# Option 2: Use individual parameters
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=netzertech

# SSL Configuration (set to 'false' to disable SSL)
DB_SSL=true

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here

# Application Configuration
NODE_ENV=development
PORT=3000

# CORS Configuration (comma-separated list of allowed origins)
# Example: CORS_ORIGINS=http://localhost:3000,https://app.example.com
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173

# Rate Limiting Configuration
# Users can make 10 requests per minute. If exceeded, they are blocked for 30 minutes.
# THROTTLE_BURST_TTL: Burst window in milliseconds (default: 60000 = 1 minute)
# THROTTLE_BURST_LIMIT: Maximum requests in burst window (default: 10)
# THROTTLE_COOLDOWN_TTL: Cooldown period in milliseconds (default: 1800000 = 30 minutes)
# THROTTLE_COOLDOWN_LIMIT: Limit during cooldown (default: 10)
THROTTLE_BURST_TTL=60000
THROTTLE_BURST_LIMIT=10
THROTTLE_COOLDOWN_TTL=1800000
THROTTLE_COOLDOWN_LIMIT=10

# Resend Email Configuration
RESEND_API_KEY=re_123456789
EMAIL_FROM=NetzerTech <onboarding@resend.dev>

# Frontend Configuration
FRONTEND_URL=http://localhost:3000